
// This file is no longer in use.
// The iron-session library has been removed to resolve deployment issues.
// A new authentication system, likely based on Firebase Auth, will replace this.

export interface SessionData {
  isLoggedIn?: boolean;
}
